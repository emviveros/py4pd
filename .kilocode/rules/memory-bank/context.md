# context.md

Qual o foco atual do trabalho?
- Prioridade máxima na finalização do script de build para Windows ([`scripts/build_all.ps1`](scripts/build_all.ps1:1)), que está parcialmente implementado e contém pendências críticas.
- Scripts de setup e limpeza estão completos e funcionais em todas as plataformas.
- Scripts de build para Linux e para macOS serão mantidos separados; o script de build para Linux está completo, e o de macOS será revisado e ajustado separadamente, separando as responsabilidades dos scripts para Linux e MacOS em scripts separados.
- Ainda faltam ajustes e validação nos scripts de build para Windows, e posteriormente será necessário revisar os scripts de build para macOS para garantir alinhamento total.
- O objetivo imediato é garantir automação de build funcional e reprodutível no Windows antes de avançar para outras plataformas.